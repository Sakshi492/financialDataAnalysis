import React from 'react';
import HOMEPAGE_LITERALS from '../../../constants/homepage';

const CorrelationDefinition = () => {
  return (
    <div className="correlation-definition">
      {HOMEPAGE_LITERALS.CORRELATION_DEFINITION}
    </div>
  );
};

export default CorrelationDefinition;
